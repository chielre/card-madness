/** Language used when a pack has no card file for the requested language. */
export const DEFAULT_LANGUAGE = "nl"

/** Max number of :answer slots a black card may have to be playable. */
export const MAX_ANSWER_SLOTS = 3
